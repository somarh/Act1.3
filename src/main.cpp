#include <iostream>
#include <vector>

#include "Event.cpp"

void printVector(std::vector<int> arr) {
    for (int item: arr) {
        std::cout << item << " ";
    }
    std::cout << "\n";
}

int main() {
}